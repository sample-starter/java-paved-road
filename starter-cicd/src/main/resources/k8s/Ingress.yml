apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-spring-boot-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: my-spring-boot-app.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-spring-boot-app-service
                port:
                  number: 80